{
  "name": "electionassist-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "postinstall": "npm install --prefix packages/backend",
    "start": "npm start --prefix packages/backend"
  }
}
